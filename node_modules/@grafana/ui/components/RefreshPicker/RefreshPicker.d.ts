import { PureComponent } from 'react';
import { SelectOptionItem } from '../Select/Select';
export declare const offOption: {
    label: string;
    value: string;
};
export declare const defaultIntervals: string[];
export interface Props {
    intervals?: string[];
    onRefresh: () => any;
    onIntervalChanged: (interval: string) => void;
    value?: string;
    tooltip: string;
}
export declare class RefreshPicker extends PureComponent<Props> {
    static defaultProps: {
        intervals: string[];
    };
    constructor(props: Props);
    hasNoIntervals: () => boolean;
    intervalsToOptions: (intervals?: string[]) => SelectOptionItem<string>[];
    onChangeSelect: (item: SelectOptionItem<string>) => void;
    render(): JSX.Element;
}
