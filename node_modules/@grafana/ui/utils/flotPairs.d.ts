import { NullValueMode, GraphSeriesValue, SeriesData } from '../types/index';
export interface FlotPairsOptions {
    series: SeriesData;
    xIndex: number;
    yIndex: number;
    nullValueMode?: NullValueMode;
}
export declare function getFlotPairs({ series, xIndex, yIndex, nullValueMode }: FlotPairsOptions): GraphSeriesValue[][];
